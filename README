# Quick setup steps

Select a user/password and update /etc/bmgr/bmgr.conf

$ mysql
  > grant all privileges on bmgr.* to bmgr_user@'localhost' identified by 'bmgr_pass';
  > create database bmgr;

$ python
  > from bmgr.server import init_db;
  > init_db()

$ echo 'WSGIScriptAlias /bmgr "/var/www/bmgr/bmgr.wsgi"' >> /etc/httpd/conf/httpd.conf

$ systemctl restart httpd

$ bmgr profile_setattr profile1 attr1 value1

$ bmgr node_setprof node[100-200] profile1

$ bmgr profile_list

$ bmgr node_show node100

Configure templates in /etc/bmgr/templates
